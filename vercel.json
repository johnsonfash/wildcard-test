{
	"version": 2,
	"buildCommand": "npm run build",
	"installCommand": "npm install",
	"devCommand": "npm start",
	"outputDirectory": ".",
	"builds": [
		{
			"src": "server.js",
			"use": "@vercel/node"
		}
	],
	"routes": [
		{
			"src": "/(.*)",
			"dest": "server.js"
		}
	]
}